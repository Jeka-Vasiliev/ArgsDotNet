namespace ArgsDotNet
{
    internal enum ErrorCode
    {
        INVALID_ARGUMENT_FORMAT,
        INVALID_ARGUMENT_NAME,
        UNEXPECTED_ARGUMENT,
        MISSING_INTEGER,
        INVALID_INTEGER,
        MISSING_STRING
    }
}